# Agent Outputs

Each specialist writes a short completion note here after a team run.
The orchestrator and the reviewer read these instead of loading each agent's full context.

- `ecoshift-frontend.md` — frontend changes + endpoint contracts consumed
- `ecoshift-backend.md`  — Lambda/SAM changes + endpoint contracts produced
- `ecoshift-reviewer.md` — cross-check + verdict
